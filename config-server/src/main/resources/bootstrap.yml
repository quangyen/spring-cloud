spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        monitor:

        git:
#          uri: file://${HOME}/config-repo
#          uri: https://github.com/quangyen/springcloud-config.git
#          searchPaths: config
          uri: file://C:/server-config
          clone-on-start: true
        bootstrap: true